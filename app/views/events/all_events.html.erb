
<%= link_to 'home page', root_path %>
<%=link_to "show my events", user_path(:current) %>

<h1>Listing events</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Content</th>
    <th>Date</th>
    <th>user</th>
    <th>Id repeat</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

  <% @events.each do |event| %>
    <tr>
      <td><%= event.name %></td>
      <td><%= event.text %></td>
      <td><%= event.date %></td>


      <td><%if event.user_id == current_user.id%>
          <%= "#{current_user.name}"%></td>
      <%else%>
        <%="anonimus"%>
      <%end%>



      <td><%= event.repeat_id %></td>
  
    </tr>
  <% end %>
</table>

<br />

<%= link_to 'New Event', new_event_path %>
<%= link_to 'show calendar', calendar_path %>